name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-macos:
    strategy:
      matrix:
        include:
          - os: macos-13        # Intel runner (x86_64)
            arch: x86_64
            name: Intel
          - os: macos-14        # Apple Silicon runner (arm64)
            arch: arm64
            name: AppleSilicon
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.x'
          channel: 'stable'
          cache: true

      - name: Install Transmission (for bundling)
        run: |
          brew install transmission

      - name: Bundle Transmission binary
        run: |
          mkdir -p gui/macos/Runner/Resources/bin
          cp $(which transmission-daemon) gui/macos/Runner/Resources/bin/
          chmod +x gui/macos/Runner/Resources/bin/transmission-daemon

          # Copy required dylibs
          mkdir -p gui/macos/Runner/Resources/lib

          # Get Transmission dependencies (excluding system libs)
          DYLIBS=$(otool -L $(which transmission-daemon) | grep -v '/usr/lib' | grep -v '/System' | awk '{print $1}' | tail -n +2)

          for dylib in $DYLIBS; do
            if [ -f "$dylib" ]; then
              cp "$dylib" gui/macos/Runner/Resources/lib/
            fi
          done

          # Fix library paths to be relative (CRITICAL for user machines)
          chmod +w gui/macos/Runner/Resources/bin/transmission-daemon
          install_name_tool -change /opt/homebrew/opt/libevent/lib/libevent-2.1.7.dylib @executable_path/../Resources/lib/libevent-2.1.7.dylib gui/macos/Runner/Resources/bin/transmission-daemon || true
          install_name_tool -change /opt/homebrew/opt/miniupnpc/lib/libminiupnpc.21.dylib @executable_path/../Resources/lib/libminiupnpc.21.dylib gui/macos/Runner/Resources/bin/transmission-daemon || true

          echo "✅ Transmission bundled successfully"

      - name: Download and bundle yt-dlp
        run: |
          echo "📥 Downloading yt-dlp for macOS..."
          
          # Download yt-dlp standalone binary (no Python dependency)
          # Using yt-dlp_macos instead of yt-dlp to avoid Python 3.10+ requirement
          curl -L -o gui/macos/Runner/Resources/bin/yt-dlp https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp_macos
          
          chmod +x gui/macos/Runner/Resources/bin/yt-dlp
          
          # Verify download
          ls -lh gui/macos/Runner/Resources/bin/yt-dlp
          
          echo "✅ yt-dlp bundled successfully"

      - name: Download and bundle FFprobe
        run: |
          echo "📥 Downloading FFprobe for macOS..."
          
          # Download FFprobe static binary from evermeet.cx (trusted macOS FFmpeg builds)
          # This provides EXACT audio quality reading for audiophile-grade accuracy
          curl -L -o ffprobe.7z https://evermeet.cx/ffmpeg/ffprobe-7.1.7z
          
          # Extract FFprobe from 7z archive
          7z x ffprobe.7z
          
          # Move to app bundle resources
          mv ffprobe gui/macos/Runner/Resources/bin/ffprobe
          chmod +x gui/macos/Runner/Resources/bin/ffprobe
          
          # Verify download
          ls -lh gui/macos/Runner/Resources/bin/ffprobe
          gui/macos/Runner/Resources/bin/ffprobe -version | head -n 1
          
          # Clean up archive
          rm -f ffprobe.7z
          
          echo "✅ FFprobe bundled successfully"

      - name: Install dependencies
        run: |
          cd gui
          flutter pub get

      - name: Build macOS app
        run: |
          cd gui
          flutter build macos --release

      - name: Copy bundled binaries to app bundle
        run: |
          APP_PATH="gui/build/macos/Build/Products/Release/KarmaPlayer.app"
          mkdir -p "$APP_PATH/Contents/Resources/bin"
          mkdir -p "$APP_PATH/Contents/Resources/lib"

          # Copy transmission binary and libs
          if [ -f gui/macos/Runner/Resources/bin/transmission-daemon ]; then
            cp gui/macos/Runner/Resources/bin/transmission-daemon "$APP_PATH/Contents/Resources/bin/"
            chmod +x "$APP_PATH/Contents/Resources/bin/transmission-daemon"

            # Fix library paths to use @loader_path (relative to binary, not main app)
            echo "🔧 Fixing library paths..."
            install_name_tool -change @executable_path/../Resources/lib/libevent-2.1.7.dylib @loader_path/../lib/libevent-2.1.7.dylib "$APP_PATH/Contents/Resources/bin/transmission-daemon" 2>/dev/null || true
            install_name_tool -change @executable_path/../Resources/lib/libminiupnpc.21.dylib @loader_path/../lib/libminiupnpc.21.dylib "$APP_PATH/Contents/Resources/bin/transmission-daemon" 2>/dev/null || true

            # Re-sign after modifying
            echo "🔏 Signing binary..."
            codesign --force --sign - "$APP_PATH/Contents/Resources/bin/transmission-daemon"
          fi

          # Copy yt-dlp binary
          if [ -f gui/macos/Runner/Resources/bin/yt-dlp ]; then
            cp gui/macos/Runner/Resources/bin/yt-dlp "$APP_PATH/Contents/Resources/bin/"
            chmod +x "$APP_PATH/Contents/Resources/bin/yt-dlp"
            
            # Sign the binary
            echo "🔏 Signing yt-dlp binary..."
            codesign --force --sign - "$APP_PATH/Contents/Resources/bin/yt-dlp"
            
            echo "✅ yt-dlp copied and signed"
          fi

          # Copy FFprobe binary
          if [ -f gui/macos/Runner/Resources/bin/ffprobe ]; then
            cp gui/macos/Runner/Resources/bin/ffprobe "$APP_PATH/Contents/Resources/bin/"
            chmod +x "$APP_PATH/Contents/Resources/bin/ffprobe"
            
            # Sign the binary
            echo "🔏 Signing FFprobe binary..."
            codesign --force --sign - "$APP_PATH/Contents/Resources/bin/ffprobe"
            
            echo "✅ FFprobe copied and signed"
          fi

          if [ -d gui/macos/Runner/Resources/lib ]; then
            # Copy dylibs if they don't exist (avoid permission errors)
            for dylib in gui/macos/Runner/Resources/lib/*.dylib; do
              dylib_name=$(basename "$dylib")
              if [ ! -f "$APP_PATH/Contents/Resources/lib/$dylib_name" ]; then
                cp "$dylib" "$APP_PATH/Contents/Resources/lib/" || true
              fi
            done

            # Sign dylibs
            for dylib in "$APP_PATH/Contents/Resources/lib/"*.dylib; do
              [ -f "$dylib" ] && codesign --force --sign - "$dylib" 2>/dev/null || true
            done
          fi

          # Verify transmission works
          echo "🧪 Testing transmission binary..."
          "$APP_PATH/Contents/Resources/bin/transmission-daemon" --version || echo "⚠️ Transmission test failed"

          echo "✅ All binaries copied to app bundle"

      - name: Re-sign app bundle
        run: |
          APP_PATH="gui/build/macos/Build/Products/Release/KarmaPlayer.app"
          
          echo "🔏 Re-signing app bundle after modifications..."
          
          # Re-sign the entire bundle with ad-hoc signature
          # --force: Replace existing signature (which was invalidated by modifications)
          # --deep: Sign all nested code (binaries, frameworks, etc.)
          # --sign -: Use ad-hoc signing (no certificate needed)
          codesign --force --deep --sign - "$APP_PATH"
          
          # Verify signature
          echo "✅ Verifying signature..."
          codesign --verify --deep --strict --verbose=2 "$APP_PATH" 2>&1 || true
          
          echo "✅ App bundle re-signed successfully"

      - name: Create ZIP
        run: |
          cd gui/build/macos/Build/Products/Release
          
          # Copy install instructions from repo root
          cp ../../../../../../MACOS_INSTALL.txt .
          
          # Create ZIP with app and instructions (architecture-specific naming)
          ditto -c -k --sequesterRsrc --keepParent KarmaPlayer.app ../../../../../KarmaPlayer-macOS-${{ matrix.name }}.zip
          
          # Add install instructions to ZIP
          zip -u ../../../../../KarmaPlayer-macOS-${{ matrix.name }}.zip MACOS_INSTALL.txt
          
          cd ../../../../..

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: KarmaPlayer-macOS-${{ matrix.name }}
          path: gui/KarmaPlayer-macOS-${{ matrix.name }}.zip

  build-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.x'
          channel: 'stable'
          cache: true

      - name: Download Transmission for Windows
        shell: bash
        run: |
          TRANSMISSION_VERSION="4.0.5"
          DOWNLOAD_URL="https://github.com/transmission/transmission/releases/download/${TRANSMISSION_VERSION}/transmission-${TRANSMISSION_VERSION}-x64.msi"

          echo "📥 Downloading Transmission..."
          curl -L -o transmission.msi "$DOWNLOAD_URL"

          if [ ! -f transmission.msi ]; then
            echo "❌ Failed to download transmission.msi"
            exit 1
          fi

          echo "✅ Downloaded $(ls -lh transmission.msi)"

          # Extract MSI using 7-zip (pre-installed on GitHub Actions Windows)
          echo "📦 Extracting MSI with 7-zip..."
          EXTRACT_DIR="transmission-extracted"
          mkdir -p "$EXTRACT_DIR"

          # Extract MSI archive with 7-zip
          7z x transmission.msi -o"$EXTRACT_DIR" -y

          echo "✅ MSI extraction complete"

          # Show what was extracted
          echo "📂 Extracted contents:"
          find "$EXTRACT_DIR" -type f \( -name "*.exe" -o -name "*.dll" \) | head -20

          # Find the binaries in extracted directory
          # Windows MSI uses underscore (transmission_daemon.exe), but we need hyphen for consistency
          DAEMON_PATH=$(find "$EXTRACT_DIR" -name "transmission_daemon.exe" -type f | head -1)

          if [ -z "$DAEMON_PATH" ]; then
            echo "❌ transmission_daemon.exe not found in extracted files"
            echo "📋 Searching for all executables:"
            find "$EXTRACT_DIR" -name "*.exe" -type f
            exit 1
          fi

          echo "✅ Found transmission_daemon.exe at: $DAEMON_PATH"
          DAEMON_DIR=$(dirname "$DAEMON_PATH")

          # List what's in the directory
          echo "📂 Contents of daemon directory:"
          ls -la "$DAEMON_DIR"

          # Copy daemon and required DLLs
          mkdir -p gui/windows-binaries
          cp "$DAEMON_PATH" gui/windows-binaries/

          # Rename from underscore to hyphen (transmission_daemon.exe -> transmission-daemon.exe)
          mv gui/windows-binaries/transmission_daemon.exe gui/windows-binaries/transmission-daemon.exe
          echo "✅ Copied and renamed to transmission-daemon.exe"

          # Copy all DLLs from same directory
          cp "$DAEMON_DIR"/*.dll gui/windows-binaries/ 2>/dev/null || echo "⚠️  No DLLs in daemon directory"

          # Also check for DLLs in parent directories
          PARENT_DIR=$(dirname "$DAEMON_DIR")
          cp "$PARENT_DIR"/*.dll gui/windows-binaries/ 2>/dev/null || true

          # Rename all DLLs with underscores to hyphens (Windows Transmission MSI uses underscores)
          echo "🔄 Renaming DLLs from underscore to hyphen format..."
          cd gui/windows-binaries
          for file in *_*.dll; do
            if [ -f "$file" ]; then
              newname=$(echo "$file" | tr '_' '-')
              mv "$file" "$newname"
              echo "  Renamed: $file -> $newname"
            fi
          done
          cd ../..

          echo "📦 Bundled files:"
          ls -la gui/windows-binaries/

      - name: Download yt-dlp for Windows
        shell: bash
        run: |
          echo "📥 Downloading yt-dlp..."
          YTDLP_URL="https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp.exe"

          curl -L -o gui/windows-binaries/yt-dlp.exe "$YTDLP_URL"

          if [ ! -f gui/windows-binaries/yt-dlp.exe ]; then
            echo "❌ Failed to download yt-dlp.exe"
            exit 1
          fi

          echo "✅ Downloaded yt-dlp.exe ($(ls -lh gui/windows-binaries/yt-dlp.exe | awk '{print $5}'))"
          echo "📦 Final bundled files:"
          ls -la gui/windows-binaries/

      - name: Download FFprobe for Windows
        shell: bash
        run: |
          echo "📥 Downloading FFprobe..."
          
          # Download FFprobe essentials from gyan.dev (trusted Windows FFmpeg builds)
          # This provides EXACT audio quality reading for audiophile-grade accuracy
          FFPROBE_URL="https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip"
          
          curl -L -o ffmpeg-essentials.zip "$FFPROBE_URL"
          
          # Extract ffprobe.exe from the archive
          unzip -j ffmpeg-essentials.zip '*/bin/ffprobe.exe' -d gui/windows-binaries/
          
          if [ ! -f gui/windows-binaries/ffprobe.exe ]; then
            echo "❌ Failed to extract ffprobe.exe"
            exit 1
          fi
          
          # Cleanup
          rm -f ffmpeg-essentials.zip
          
          echo "✅ Downloaded ffprobe.exe ($(ls -lh gui/windows-binaries/ffprobe.exe | awk '{print $5}'))"

      - name: Install dependencies
        run: |
          cd gui
          flutter pub get

      - name: Build Windows app
        run: |
          cd gui
          flutter build windows --release

      - name: Bundle Transmission with app
        shell: bash
        run: |
          BUILD_DIR="gui/build/windows/x64/runner/Release"

          echo "📂 Build directory: $BUILD_DIR"
          if [ ! -d "$BUILD_DIR" ]; then
            echo "❌ Build directory not found!"
            exit 1
          fi

          echo "📦 Pre-bundle contents:"
          ls -la "$BUILD_DIR"

          # Copy Transmission binaries to app directory
          if [ -d "gui/windows-binaries" ]; then
            echo "📥 Copying transmission binaries..."
            cp -v gui/windows-binaries/* "$BUILD_DIR/"
            echo "✅ Transmission bundled with Windows app"

            echo "📦 Post-bundle contents:"
            ls -la "$BUILD_DIR" | grep transmission
          else
            echo "❌ No bundled binaries found at gui/windows-binaries"
            exit 1
          fi

      - name: Create ZIP
        shell: bash
        run: |
          RELEASE_DIR="gui/build/windows/x64/runner/Release"
          BUILD_DIR="gui/build/windows/x64/runner"

          echo "📂 Source directories:"
          echo "  Release: $RELEASE_DIR"
          echo "  Build: $BUILD_DIR"

          # Verify Release directory exists
          if [ ! -d "$RELEASE_DIR" ]; then
            echo "❌ Release directory not found!"
            exit 1
          fi

          # Create a clean distribution directory
          mkdir -p gui/windows-dist

          echo "📦 Copying application files..."

          # Copy the executable from Release folder
          cp -v "$RELEASE_DIR/KarmaPlayer.exe" gui/windows-dist/

          # Copy ALL DLLs from Release folder (Flutter runtime DLLs)
          echo "📚 Copying Flutter DLLs from Release..."
          cp -v "$RELEASE_DIR"/*.dll gui/windows-dist/ 2>/dev/null || echo "⚠️  No DLLs in Release folder"

          # Copy data folder (contains flutter_assets and ICU data) - CRITICAL
          if [ -d "$BUILD_DIR/data" ]; then
            echo "📂 Copying Flutter data folder..."
            cp -rv "$BUILD_DIR/data" gui/windows-dist/
          else
            echo "❌ data folder not found!"
            exit 1
          fi

          # Copy bundled binaries (transmission, yt-dlp, ffprobe) from Release folder
          echo "📥 Copying bundled binaries..."

          if [ -f "$RELEASE_DIR/transmission-daemon.exe" ]; then
            cp -v "$RELEASE_DIR"/transmission-*.exe gui/windows-dist/
            cp -v "$RELEASE_DIR"/transmission-*.dll gui/windows-dist/
            echo "✅ Transmission bundled"
          else
            echo "⚠️  transmission-daemon.exe not found in Release"
          fi

          if [ -f "$RELEASE_DIR/yt-dlp.exe" ]; then
            cp -v "$RELEASE_DIR/yt-dlp.exe" gui/windows-dist/
            echo "✅ yt-dlp bundled"
          else
            echo "⚠️  yt-dlp.exe not found in Release"
          fi

          if [ -f "$RELEASE_DIR/ffprobe.exe" ]; then
            cp -v "$RELEASE_DIR/ffprobe.exe" gui/windows-dist/
            echo "✅ ffprobe bundled"
          else
            echo "⚠️  ffprobe.exe not found in Release"
          fi

          echo ""
          echo "📦 Final Windows distribution contents:"
          ls -lah gui/windows-dist/
          echo ""
          echo "📂 Data folder check:"
          ls -la gui/windows-dist/data/ || echo "❌ No data folder!"
          echo ""
          echo "💾 Total size:"
          du -sh gui/windows-dist/

          # Create ZIP from the distribution directory
          cd gui/windows-dist
          7z a -tzip ../KarmaPlayer-Windows.zip *

          echo ""
          echo "✅ Created KarmaPlayer-Windows.zip"
          cd ..
          ls -lh KarmaPlayer-Windows.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: KarmaPlayer-Windows
          path: gui/KarmaPlayer-Windows.zip

  build-linux:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.x'
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev liblzma-dev libmpv-dev mpv transmission-daemon libcurl4-openssl-dev
          cd gui
          flutter pub get

      - name: Bundle Transmission binary
        run: |
          mkdir -p gui/linux-binaries/bin

          # Copy transmission-daemon
          cp $(which transmission-daemon) gui/linux-binaries/bin/
          chmod +x gui/linux-binaries/bin/transmission-daemon

          echo "✅ Transmission binary prepared for Linux"

      - name: Download and bundle yt-dlp (Linux)
        run: |
          echo "📥 Downloading yt-dlp for Linux..."
          
          # Download yt-dlp standalone binary (no Python dependency)
          # Using yt-dlp_linux instead of yt-dlp to avoid Python 3.10+ requirement
          mkdir -p gui/linux-binaries/bin
          curl -L -o gui/linux-binaries/bin/yt-dlp https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp_linux
          
          chmod +x gui/linux-binaries/bin/yt-dlp
          
          # Verify download
          ls -lh gui/linux-binaries/bin/yt-dlp
          
          echo "✅ yt-dlp bundled successfully"

      - name: Download and bundle FFprobe (Linux)
        run: |
          echo "📥 Downloading FFprobe for Linux..."

          # Download FFprobe static binary from BtbN/FFmpeg-Builds (GitHub mirror, more reliable)
          # This provides EXACT audio quality reading for audiophile-grade accuracy
          mkdir -p gui/linux-binaries/bin

          # Download and extract FFmpeg static build (includes FFprobe)
          # Using BtbN builds which are reliable and hosted on GitHub
          curl -L -o ffmpeg-linux.tar.xz https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-linux64-gpl.tar.xz
          tar -xJf ffmpeg-linux.tar.xz

          # Find and move ffprobe binary (it's in a versioned directory)
          find . -name ffprobe -type f -executable -exec cp {} gui/linux-binaries/bin/ffprobe \;
          chmod +x gui/linux-binaries/bin/ffprobe

          # Verify download
          ls -lh gui/linux-binaries/bin/ffprobe
          gui/linux-binaries/bin/ffprobe -version | head -n 1

          # Clean up
          rm -rf ffmpeg-*
          
          echo "✅ FFprobe bundled successfully"

      - name: Build Linux app
        run: |
          cd gui
          flutter build linux --release

      - name: Bundle binaries with app
        run: |
          BUILD_DIR="gui/build/linux/x64/release/bundle"

          # Copy Transmission to bundle
          mkdir -p "$BUILD_DIR/bin"
          if [ -f gui/linux-binaries/bin/transmission-daemon ]; then
            cp gui/linux-binaries/bin/transmission-daemon "$BUILD_DIR/bin/"
            chmod +x "$BUILD_DIR/bin/transmission-daemon"
            echo "✅ Transmission bundled with Linux app"
          fi

          # Copy yt-dlp to bundle
          if [ -f gui/linux-binaries/bin/yt-dlp ]; then
            cp gui/linux-binaries/bin/yt-dlp "$BUILD_DIR/bin/"
            chmod +x "$BUILD_DIR/bin/yt-dlp"
            echo "✅ yt-dlp bundled with Linux app"
          fi

          # Copy FFprobe to bundle
          if [ -f gui/linux-binaries/bin/ffprobe ]; then
            cp gui/linux-binaries/bin/ffprobe "$BUILD_DIR/bin/"
            chmod +x "$BUILD_DIR/bin/ffprobe"
            echo "✅ FFprobe bundled with Linux app"
          fi

      - name: Create TAR.GZ
        run: |
          cd gui
          tar -czf KarmaPlayer-Linux.tar.gz -C build/linux/x64/release/bundle .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: KarmaPlayer-Linux
          path: gui/KarmaPlayer-Linux.tar.gz

  create-release:
    needs: [build-macos, build-windows, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Extract version from pubspec.yaml
        id: version
        run: |
          VERSION=$(grep '^version:' gui/pubspec.yaml | awk '{print $2}' | cut -d'+' -f1)
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "📦 Extracted version: ${VERSION}"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/KarmaPlayer-macOS-Intel/*
            artifacts/KarmaPlayer-macOS-AppleSilicon/*
            artifacts/KarmaPlayer-Windows/*
            artifacts/KarmaPlayer-Linux/*
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## 🎵 KarmaPlayer Release

            **AI-powered music discovery with built-in player**

            ### ⚠️ IMPORTANT: macOS Installation

            **1. Choose the Correct Version for Your Mac:**
            
            - Click Apple menu () → **About This Mac**
            - Look at the **Chip** or **Processor** line:
              - **Intel** processor (2020 and earlier) → Download **Intel** version
              - **Apple M1/M2/M3** (2020+) → Download **AppleSilicon** version

            **2. First Launch Security Warning:**
            
            macOS will show a security warning on first launch. This is normal for open-source apps.

            **To fix (choose ONE method):**

            1. **Right-click method** (easiest): Right-click `KarmaPlayer.app` → Open → Click "Open" in dialog
            2. **Terminal method**: Run `xattr -cr /path/to/KarmaPlayer.app`

            **Why?** Apple requires a $99/year Developer Program to avoid this. As an open-source project, we don't pay for that. The app is safe - you can review the code on GitHub.

            📄 See `MACOS_INSTALL.txt` (included in download) for detailed instructions.

            ### 🎉 What's New in v${{ steps.version.outputs.version }}

            - 🏗️ **Architecture-specific macOS builds** - Fixed "Bad CPU type" errors on Intel Macs
            - 🐛 **Fixed Flutter engine SIGPIPE on older macOS** - Big Sur (11.6.1) compatibility
            - 🐛 **Fixed SIGPIPE crashes** - No more random crashes during playback
            - 🐛 **Fixed MPV memory crashes** - Improved lifecycle management
            - 📖 **macOS installation guide** - Clear instructions for first-time setup
            - ✅ **Bundled Transmission** - No separate installation needed!

            ### Downloads

            **macOS - Intel (x86_64)**
            - For Intel-based Macs (2020 and earlier)
            - Download: `KarmaPlayer-macOS-Intel.zip`

            **macOS - Apple Silicon (ARM64)**
            - For M1, M2, M3+ Macs (2020 and newer)
            - Download: `KarmaPlayer-macOS-AppleSilicon.zip`

            **Windows**
            - Download the `.zip` file, extract and run

            **Linux**
            - Download the `.tar.gz` file, extract and run

            ### Features
            - 🎨 Modern GUI with responsive design
            - 📊 Statistics badges (albums, plays, downloads)
            - 🎵 Built-in music player with MPV
            - ⭐ Favorites and ratings for tracks
            - 🌐 Connection quality indicator
            - 🔍 AI-powered music discovery

            ### Requirements
            - **macOS**: 10.15 (Catalina) or later
            - **Windows**: Windows 10/11
            - **Linux**: Ubuntu 20.04+ / Debian 11+ / Fedora 35+

            ### Quick Start
            1. Download for your platform
            2. **macOS users**: Follow installation instructions above
            3. Extract and launch KarmaPlayer
            4. Search for music (try: "radiohead ok computer flac")
            5. Click download - Transmission starts automatically!
            6. Enjoy your high-quality music!

            ---

            *Report bugs or request features on [GitHub Issues](https://github.com/trust-tune-net/karma-player/issues)*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
